# Copy this to .env (gitignored — see repo .gitignore's `.env` rule) and fill
# in your real API Gateway URL. Never commit the real value.
API_URL=https://your-api-id.execute-api.us-east-1.amazonaws.com/prod/dashboard
DISPLAY_NAME=Adam
